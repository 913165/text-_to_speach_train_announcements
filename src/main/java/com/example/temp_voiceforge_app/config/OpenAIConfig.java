package com.example.temp_voiceforge_app.config;

import org.springframework.context.annotation.Configuration;

/**
 * OpenAI configuration placeholder.
 * Spring AI auto-configures the OpenAiAudioSpeechModel bean via
 * spring-ai-autoconfigure-model-openai using the properties defined in
 * application.properties (spring.ai.openai.api-key, etc.).
 *
 * Add any custom bean overrides here if needed.
 */
@Configuration
public class OpenAIConfig {
    // Spring AI auto-configuration handles OpenAiAudioSpeechModel bean creation.
    // Override beans here only if custom configuration is required.
}

